import React from 'react';
import { Card, CardBody, CardTitle, CardImg } from 'reactstrap';

export const PhotoCard = () => {
    return (
        <Card>
            <CardImg />
            <CardBody>
                <CardTitle></CardTitle>
            </CardBody>
        </Card>
    )
}